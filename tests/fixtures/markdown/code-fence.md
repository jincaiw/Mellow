<!-- fixture: code-fence.md — 代码块与语言标注 -->

```ts
const editor = new EditorView({ doc: '# Title' });
```

```rust
fn main() { println!("hello"); }
```

```python
def hello():
    return "world"
```

无语言标注：

```
plain block
```

行内代码 `const x = 1;` 与代码块共存。

```json
{ "key": "value" }
```
