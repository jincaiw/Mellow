# Typora 对标方案审查证据：2026-09-06

- 本机应用：`/Applications/Typora.app`，版本 1.14.9，build 7785；通过应用包版本信息核实。
- `typora-view-menu.ax.txt`：本机“显示”菜单的辅助功能树，证明菜单中存在“显示标签页栏”“显示所有标签页”“文档列表”“文件树”等入口。
- 点击“文档列表”后，辅助功能树没有提供足够的侧边栏状态证据。因此不宣称已验证列表布局、排序或交互。
- 原生截图请求返回 `Screenshot unavailable for /Applications/Typora.app.`，没有可用的视觉截图。像素尺寸、字体、默认布局、悬停状态和动画尚未进行截图对照验收。
- 未修改 Typora 中已有文档的正文；不保留与任务无关的文档正文辅助功能树。
- 菜单入口存在不代表默认启用，也不能证明新建／打开／关闭的完整窗口和标签语义。
- 本证据仅支撑待确认方案，不是发布验收报告。

## 本轮文档验证

- `git diff --check`：通过。
- `node tests/parity/verify-parity-ledger.mjs`：通过；现有台账仍为 32 个 P0 项，IMPL=1、AUTO=28、MAC=2、NOT_TESTED=1。这不代表新方案的 88 项已验收。
- 确认稿结构检查：88 个能力 ID 无重复，30 个 Golden Journeys，评分权重合计 100，本地 Markdown 链接目标存在。
- 本轮未执行完整产品测试、原生视觉对照、Undo／Redo、IME 或三平台发布验收，因为用户要求先提交方案、确认后实施。
