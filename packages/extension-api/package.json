{
  "name": "@mellow/extension-api",
  "version": "0.1.0",
  "description": "扩展 API 契约（PRD §119）",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "../settings/node_modules/.bin/jest --rootDir ."
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
